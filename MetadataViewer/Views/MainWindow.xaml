<Window x:Class="MetadataViewer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MetadataViewer.Views"
        xmlns:action="clr-namespace:MetadataViewer.Views.Actions"
        xmlns:aprop="clr-namespace:MetadataViewer.Views.AttachedProperties"
        mc:Ignorable="d"
        Title="MetadataViewer"
        Height="450" Width="800" >

    <DockPanel Margin="10" >
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Drop" >
                <action:GetDroppedFilePathAction DroppedPath="{Binding DroppedPath.Value, Mode=OneWayToSource}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>

        <TextBlock DockPanel.Dock="Top"
                   Text="{Binding FilePath.Value, Mode=OneWay}" />

        <DataGrid CanUserReorderColumns="False" CanUserSortColumns="False"
                  ItemsSource="{Binding Tags.Value, Mode=OneWay}"
                  aprop:DataGridFormat.IntFormatAutoGenerate="0x{0:x4}" />
    </DockPanel>
</Window>
