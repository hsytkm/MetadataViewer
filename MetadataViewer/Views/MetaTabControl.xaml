<UserControl x:Class="MetadataViewer.Views.MetaTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MetadataViewer.Views"
             xmlns:aprop="clr-namespace:MetadataViewer.Views.AttachedProperties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >

    <TabControl TabStripPlacement="Left"
                ItemsSource="{Binding Pages, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:MetaTabControl}}}" >
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Name, Mode=OneTime}" />
            </DataTemplate>
        </TabControl.ItemTemplate>

        <TabControl.ContentTemplate>
            <DataTemplate>
                <local:MetaDataGrid Tags="{Binding Tags, Mode=OneWay}" />
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>

</UserControl>
